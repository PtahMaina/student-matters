header.header
  nav.nav.nav--tours
    a.nav__el(href='/') Dekut
  .header__logo
    img(src='/img/dekut.jpeg' alt='Dekut logo')
    h2 Dedan Kimathi University of Technology
    h3 Better Life Through Technology
  nav.nav.nav--user
    if user
      a.nav__el.nav__el--logout Log out
      a.nav__el(href='/me')
        img.nav__user-img(src=`/img/users/${user.photo}` alt=`Photo of ${user.name}`)
        span= user.name.split(' ')[0]
    else
      a.nav__el(href='/login') Log in
      a.nav__el.nav__el--cta(href='/signup') Sign up